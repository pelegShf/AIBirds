def decode_byte_to_int(byte_data):
    """Takes list of bytes returns list of ints.

       This function decodes each byte to its correct int.

       :param bytes byte_data: list of bytes.
       :returns:  list of corresponding ints.
       :rtype: list
       """
    int_list = list(byte_data)
    return int_list
